/**
 * @author Juan Lee
 * @since 2023-07-01
 */

export default {
    /* Initalize Function Start */
    /**
     * @param {Object} mail
     * @param {Object} web
     */
    init: function initalize(mail, web) {
        console.log(mail.ar, web)
    }
}